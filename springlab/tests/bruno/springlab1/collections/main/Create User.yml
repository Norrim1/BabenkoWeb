info:
  name: Create User
  type: http
  seq: 4

http:
  method: POST
  url: http://{{base_url}}/greeting
  body:
    type: json
    data: |-
      {
        "name": "Danil",
        "surname": "Babenko"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
