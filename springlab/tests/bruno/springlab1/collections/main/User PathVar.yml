info:
  name: User PathVar
  type: http
  seq: 3

http:
  method: GET
  url: http://{{base_url}}/greeting/{{test_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
